{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "sidecar-capability",
  "description": "Sidecar execution for nest-music-server",
  "windows": ["main"],
  "permissions": [
    {
      "identifier": "shell:allow-execute",
      "allow": [{ "name": "binaries/nest-music-server", "sidecar": true }]
    }
  ]
}
